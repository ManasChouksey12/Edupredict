# Used by the Ask / Doubt solver tab (optional). Get a key: https://aistudio.google.com/apikey
# Free tier: ~3 cloud answers per browser session are enforced in the app to reduce quota errors.
VITE_GEMINI_API_KEY=

# Optional model
# VITE_GEMINI_MODEL=gemini-2.0-flash
